/** Shared focus ring for nav controls (dark navbar + light drawers). */
export const navFocusRing =
  'focus:outline-none focus-visible:ring-2 focus-visible:ring-accent focus-visible:ring-offset-2 focus-visible:ring-offset-bg';

/** Mobile-friendly touch target; desktop may stay compact. */
export const navTouchTarget = 'min-h-11 min-w-11 md:min-h-9 md:min-w-9';
