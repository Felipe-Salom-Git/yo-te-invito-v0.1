# Slice 8.7 — Historial cambio de fecha

**Estado:** Implementado

- `dateChangeHistory` + `canChangeDate` en `MeTicketDetail`
- Bloque historial en `TicketDateChangePanel`
- Orden: `hasDateChangeApplied` en tickets de `GET /public/orders/:id`
